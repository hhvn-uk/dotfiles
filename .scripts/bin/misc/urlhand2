#!/bin/sh

tor=$(printf "no\nyes" | dmenu -i -p "Tor?")

hand=$(printf "mpvms video
mpvms
popupmpv
youtube-dl --add-metadata -ic
youtube-dl --add-metadata -xic
surf -e $(cat /tmp/tabbed.xid || echo 0x0)
toxclip
netimg
netvid
popupcurl
popupcgo" | dmenu -i)

[ "$tor" = "yes" ] && sh -c "torsocks $hand $1" || sh -c "$hand $1"
