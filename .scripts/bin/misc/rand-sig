#!/bin/sh

(
	awk -v "user=$(whoami)" -F":" '$1 == user {print $5}' < /etc/passwd
	sed '/^#/d' < $HOME/.config/neomutt/signatures | shuf | head -n 1 | tr '\t' '\n' | fold -sw 80 | sed 's/^/>/'
) > /tmp/signature
echo /tmp/signature
