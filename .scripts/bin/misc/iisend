#!/bin/sh

getchan(){
	chans=$(find . -name "*in" | sed 's~/in$~~;s~.*/~~')
	printf "$chans\n\n"
}

while true
do
	clear
	getchan
	printf "channel>>"; read var < /dev/tty
	printf "message>>"; read var2 < /dev/tty

	channel=$(find -type d | grep "$var")
	echo "$var2" > $channel/\in
done
