#!/bin/sh

[ -z $1 ] && echo "usage: archive-name maildir"
dir="$2"
target="$1"

cd $dir

for d in "cur" "tmp" "new"
do
	mkdir -p $target/$d \
		${target}sent/$d \
		${target}rubbish/$d

	mv inbox/$d/* $target/$d
	mv sent/$d/* ${target}sent/$d
	mv rubbish/$d/* ${target}rubbish/$d
done
