# Only good thing about readline
set editing-mode vi

# I find it more hassle than it's worth
# in e,g, gdb, where I can't use aliases
# and have to paste in commands instead.
set enable-bracketed-paste off

# I'm used to this from ksh
set mark-symlinked-directories on

# Act like ls
set colored-stats on
set visible-stats on

# Get rid of dumb interactive shit
set completion-query-items -1
set page-completions off
