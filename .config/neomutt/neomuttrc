source users/hayden@hhvn.uk	#user
#source users/hhvn@hlirc.net	#user
#source users/backtrack@airmail.cc	#user
#source users/hayden@haydenvh.com	#user
#source users/hhvn@dataswamp.org	#user

set ssl_starttls = yes
set ssl_force_tls = yes
set mailcap_path = "~/.config/neomutt/mailcap"
set smtp_authenticators = "plain"

set folder = "~/general/mail/"
set spoolfile = "+haydenvhcom"
set record = "+sent"
set postponed = "+drafts"
set trash = "+rubbish"
mailboxes =haydenvhcom =postmaster =hlircnet =lists =dev =matrix =security =news =money =dataswamporg =inbox-airmail =school =forwards =sent =drafts =rubbish =backup =spam =spam-domain

set mbox_type = Maildir
set abort_key = "<Esc>"
set abort_nosubject = no
set abort_unmodified = no
set alias_file = "~/.config/neomutt/alias"

source colours
source sidebar
source alias
source gpg

bind index D purge-message
bind index,pager M compose-to-sender
#bind index R source
bind index \Ct collapse-thread
bind index \Ca collapse-all

bind compose F edit-from

set sort=threads
set collapse_all
set help=no

set signature=~/.config/neomutt/signatures

macro index S <save-message>=spam<enter><enter>
macro index A <save-message>=spam-domain<enter><enter>

my_hdr X-Mailer: neomutt
hdr_order Subject From To Date

#folder-hook sent

set header_cache = "~/general/mail/cache/"
set message_cachedir = "~/general/mail/cache/"
